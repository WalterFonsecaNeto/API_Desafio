services:
  - type: web
    name: SistemagerenciamentoDeComandas
    env: dotnet
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/SistemagerenciamentoDeComandas.dll
    autoDeploy: true
